# Restaurant Table Reservation Frontend

React and Vite frontend for the Restaurant Table Reservation Management System.

## Local development

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

The frontend reads its backend API address from `VITE_API_URL`.
